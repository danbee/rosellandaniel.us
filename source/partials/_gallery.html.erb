<ul class="image-gallery">
  <% images.each do |image| %>
    <%= partial "partials/gallery_image", locals: { image: image } %>
  <% end %>
</ul>

<% images.each do |image| %>
  <%= partial "partials/lightbox", locals: { image: image } %>
<% end %>
